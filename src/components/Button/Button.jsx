import React from "react";
import "./Button.css";

const Button = ({ onClick }) => {
  return (
    <div>
      <button onClick={onClick}>FALTA ICONO DE ESTE BOTÓN</button>
    </div>
  );
};

export default Button;
